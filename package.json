{
  "name": "clinic-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "server": "json-server --watch data.json --port 10000"
  },
  "devDependencies": {
    "axios": "^1.11.0",
    "bcryptjs": "^3.0.2",
    "json-server": "^1.0.0-beta.3"
  }
}
